mixin demos()
  lastPageTitle = null;
  demos = locals.demos()
  for name in demos
    title = name.replace(/.html/g, '').replace(/-/g, ' ').replace(/ demo/, '')
    prefix = title.substring( 0, title.indexOf(' ') )
    if lastPageTitle != null && lastPageTitle != prefix
      hr
    a( href = '../build/debug/' + name, title=title) !{ title }
    lastPageTitle = prefix


mixin demosGenerated()
  lastPageTitle = null;
  generatedDemos = locals.generateddemos()
  for name in generatedDemos
    title = name.replace(/.html/g, '').replace(/-/g, ' ').replace(/ demo/, '')
    prefix = title.substring( 0, title.indexOf(' ') )
    if lastPageTitle != null && lastPageTitle != prefix
      hr
    a( href = '../build/debug/' + name, title=title) !{ title }
    lastPageTitle = prefix


mixin layouts()
  lastPageTitle = null;
  layouts = locals.layouts()
  for name in layouts
    title = name.replace(/.html/g, '').replace(/-/g, ' ').replace(/ layouts/, '')
    prefix = title.substring( 0, title.indexOf(' ') )
    if lastPageTitle != null && lastPageTitle != prefix
      hr
    a( href = '../build/debug/' + name, title=title) !{ title }
    lastPageTitle = prefix

mixin demoStyles()
  lastPageTitle = null;
  demoStyles = locals.demostyles()
  for name in demoStyles
    title = name.replace(/.html/g, '').replace(/-/g, ' ').replace(/ styles/, '')
    prefix = title.substring( 0, title.indexOf(' ') )
    if lastPageTitle != null && lastPageTitle != prefix
      hr
    a( href = '../build/debug/' + name, title=title) !{ title }
    lastPageTitle = prefix


include ../../common/html/ieconditionals.html
doctype 5

include docs-head.html
body( data-spy = "scroll", data-target = ".bs-docs-sidebar" )
    include docs-nav.html

  header.jumbotron.subhead#overview
    .container
      .grid.no-grid-at-767
        h1 Module Demos
        p Individual Module Demos.

  .container
    .grid.no-grid-at-767
      .span3
        h3 Demos
        hr
        nav.module-nav
          +demos()

      .span3
        h3 Generated Demos
        hr
        nav.module-nav
          +demosGenerated()

      .span3
        h3 Module Layouts
        hr
        nav.module-nav
          +layouts()

      .span3
        h3 Module Styles
        hr
        nav.module-nav
          +demoStyles()

  include docs-foot.html
</html>