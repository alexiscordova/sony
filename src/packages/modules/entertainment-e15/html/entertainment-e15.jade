include ../../../common/html/jade-helpers.jade
+includeCSS('entertainment-e15')

e = {"locals":locals, "data":data}

mixin benefitList(benefit)
  li.entertainment-benefit
    .entertainment-media
      .img.no-float
        span.icon-holder
          +iQImage(benefit.img, 'update-always')
      .bd
        h2.t6= benefit.title
        p.p3.hidden-phone= benefit.body


+mainElement( ['entertainment-wrapper'] )
  e = {"locals":locals, "data":data}
  .container-full-bleed.entertainment
    .container
      .grid
        .logo-container
          +iQImage(data.logo.img, 'update-always')
        .entertainment-header
          h1.t1= data.header
          .mobile-img.visible-phone
            - for (var i = 0; i < 3; i++) {
              +iQImage(data.img.movies[i], 'update-always')
            -  }
          p.p2= data.subhead
      .entertainment-content
        .grid
          .container-benefits
            .tablet-img.visible-tablet
              - for (var i = 0; i < 4; i++) {
                +iQImage(data.img.movies[i], 'visible-tablet update-always')
              - }
            ul.entertainment-benefits
                +benefitList(data.benefits.fees)
                +benefitList(data.benefits.stream)
                +benefitList(data.benefits.moviestogo)
            .btn-holder
              button.btn= data.learn
          .container-media
            - for (var i = 0; i < 6; i++) {
              +iQImage(data.img.movies[i], 'visible-desktop update-always')
            - }
